import 'package:flutter/material.dart';

class TestAlbum extends StatelessWidget {
  const TestAlbum({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      height: double.maxFinite,
      width: double.maxFinite,
    );
  }
}
